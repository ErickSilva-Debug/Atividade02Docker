name: Docker Build and Push

on:
  push:
    branches:
      - main

jobs:
  docker_build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.ericksilvad8 }}   # Seu nome de usuário no Docker Hub
        password: ${{ secrets.dckr_pat_DYylulNFt7Gi9BfsdToP4iJg_4k }}  # Seu token de acesso pessoal

    - name: Build Docker image
      run: docker build -t ericksilvad8/react-app:latest .

    - name: Push Docker image
      run: docker push ericksilvad8/react-app:latest
