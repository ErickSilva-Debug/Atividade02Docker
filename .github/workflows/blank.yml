name: Docker Build and Push

on:
  push:
    branches:
      - main

jobs:
  docker_build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.ERICKSILVAD8}}  # Verifique o nome exato do seu secret
        password: ${{ secrets.dckr_pat_DYylulNFt7Gi9BfsdToP4iJg_4k}}  # Verifique o nome exato do seu secret

    - name: Build Docker image
      run: docker build -t ericksilvad8/react-app:latest .

    - name: Push Docker image
      run: docker push ericksilvad8/react-app:latest
