name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  docker_build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.ericksilvad8 }}
          password: ${{ secrets.dckr_pat_xywKqk16ng0TVqYR173rZBSTghY}}

      - name: Build Docker image
        run: docker build -t ericksilvad8/react-app .

      - name: Push Docker image
        run: docker push ericksilvad8/react-app
